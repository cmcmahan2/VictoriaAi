# Required for the Stock Researcher tab (Claude). You already have one in your other apps.
ANTHROPIC_API_KEY=

# Optional — expands the researcher's data (full-market screening, fundamentals).
# Without it, the researcher uses free Stooq quotes for a curated watchlist + Claude reasoning.
FMP_API_KEY=
