# polybot runs on the Python 3.11 standard library + requests only.
# This keeps the bot and backtester runnable anywhere with zero build friction
# (no numpy/pandas/scipy/matplotlib needed — stats and charts are hand-rolled).
requests>=2.28

# Optional, only if you prefer parquet caches over CSV in backtest/data.py:
#   pandas>=2.0
#   pyarrow>=12.0
# Optional, only for live execution (not used by paper/backtest):
#   py-clob-client
